// reminder Worker (SKELETON) — check each upcoming event's expected artifacts and
// ping the team if something is missing. Not implemented yet. Runs on a schedule.
export default {
  async scheduled(event, env) {
    // TODO: inspect event data + artifacts; notify on gaps (email / Slack / Signal).
    console.log("reminder worker: not implemented");
  },
};
